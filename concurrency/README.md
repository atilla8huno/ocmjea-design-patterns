# Concurrency

Core `java.util.concurrent` usage. Run `./gradlew test`.

- **executor** — submit work to a thread pool and wait for the result.
- **atomic** — update a counter without locks.
- **thread_safe** — a list that several threads can write safely.
- **future** — `CompletableFuture` for async composition.
